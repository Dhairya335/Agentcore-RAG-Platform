[
  {
    "name": "rag_retrieve_documents",
    "description": "Search the user's uploaded documents for context relevant to their question. Use this tool first whenever the user asks something that could be answered by their uploaded documents. Returns the most relevant text passages with source citations. If the result is empty, answer from general knowledge.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The user's question to search for in their uploaded documents."
        },
        "tenantId": {
          "type": "string",
          "description": "The user's unique tenant ID (their Cognito sub claim). Scopes the search to their documents only."
        },
        "topK": {
          "type": "integer",
          "description": "Number of document chunks to retrieve (1-8, default 5).",
          "minimum": 1,
          "maximum": 8
        },
        "userRole": {
          "type": "string",
          "description": "The user's access role: INTERNAL (full document access, full citations) or EXTERNAL (restricted to externally-allowed content only, masked citations). Always pass this from the user's JWT claims. Default: EXTERNAL.",
          "enum": ["INTERNAL", "EXTERNAL"]
        },
        "docIds": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional. Restrict search to these specific document UUIDs only. Use when the user says 'in that document' or selects a specific document. Omit to search all tenant documents."
        },
        "collectionId": {
          "type": "string",
          "description": "Optional. Restrict search to documents in this collection. The Lambda resolves collection members from DynamoDB and applies the scope at the SQL layer."
        }
      },
      "required": ["query", "tenantId"]
    }
  }
]
